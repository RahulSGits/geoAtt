<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>FinAtt</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!--
	  Permission strings for the check-in flow. WKWebView forwards the page's
	  getUserMedia / geolocation calls to these native prompts, so the wording
	  here is what employees actually read before granting access. iOS kills the
	  app on the spot if a key is missing when its API is first touched.
	-->
	<key>NSCameraUsageDescription</key>
	<string>FinAtt uses the front camera to verify your face and confirm you are physically present when you check in or out.</string>
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>FinAtt checks your location against your assigned work site so an on-site check-in can be confirmed. Location is only read while you are checking in or out.</string>
	<!-- Profile photo and the HR CSV import both open the system file picker. -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>FinAtt lets you pick a profile picture from your photo library.</string>

	<!--
	  `audio: false` is passed to getUserMedia in FaceCheckIn, so no microphone
	  key is declared — asking for a permission the app never uses invites an
	  App Store rejection.
	-->

	<key>NSAppTransportSecurity</key>
	<dict>
		<!--
		  Production loads an https origin, so TLS stays fully enforced. This
		  single exception exists so a LAN dev URL (http://192.168.x.x:3000)
		  loads during development; Apple accepts it without justification.
		  Note the camera still will not work over plain http — WebKit gates
		  getUserMedia behind a secure context regardless of ATS.
		-->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSLocalNetworkUsageDescription</key>
	<string>FinAtt connects to a development server on your local network. This is not used in released builds.</string>
</dict>
</plist>
