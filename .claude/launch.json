{
  "version": "0.0.1",
  "//": "Every command begins `cd /` because the launcher can inherit a working directory that no longer exists — this project's folder was renamed from 'FinAtt app' to 'geoAtt', and any shell still holding the old path floods stderr with getcwd/chdir errors that bury the real failure. Plain `bash -c`, not `-lc`: a login shell runs profile scripts before the cd and hits the same problem.",
  "configurations": [
    {
      "name": "geoatt-web",
      "//": "The Next.js app. NOTE: Next 16 refuses to start when another `next dev` is running anywhere on the machine — including the unrelated checkouts at ~/Downloads/FinAtt and ~/Downloads/FinBud-Ai. If startup fails with 'Another next dev server is already running', kill the PID it names.",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "cd / && cd '/Users/rahul/Downloads/geoAtt/frontend' && exec npm run dev"
      ],
      "autoPort": true,
      "port": 3000
    },
    {
      "name": "geoatt-rn-web",
      "//": "The Expo app on its React Native Web target. Lets the splash animation and auth screens be checked in a browser without Xcode or an Android emulator.",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "cd / && cd '/Users/rahul/Downloads/geoAtt/mobile-rn' && exec npx expo start --web --port $PORT"
      ],
      "autoPort": true,
      "port": 8081
    },
    {
      "name": "geoatt-rn-dist",
      "//": "Serves the PRODUCTION web export from mobile-rn/dist, so `npm run build:web` output can be checked before it goes to hosting. Dev-server success does not prove the production bundle boots, and this reproduces the SPA rewrite that a plain static server does not.",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "cd / && exec node '/Users/rahul/Downloads/geoAtt/mobile-rn/tools/serve-dist.mjs' $PORT"
      ],
      "autoPort": true,
      "port": 8090
    }
  ]
}
